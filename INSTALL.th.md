# ติดตั้งและอัปเดต

หน้านี้เขียนให้ **agent อ่านแล้วลงมือตามได้ตรง ๆ** และให้คนที่นั่งอยู่ข้าง ๆ อ่านรู้เรื่องด้วย
ครอบสามเรื่อง: ติดตั้งครั้งแรก · อัปเดต · และสิ่งที่ต้องทำเมื่อผลลัพธ์ดูไม่เข้าที่ทันทีหลังติดตั้ง

หน้านี้ **ไม่ฝังเนื้อไฟล์ไหนไว้เลย** ตัวไฟล์จริงอยู่ที่ `template/` กับ `skills/` ในrepo นี้ —
มีสำเนาเดียว โดยตั้งใจ

English: [`INSTALL.md`](INSTALL.md) · ภาพรวม repo: [`README.th.md`](README.th.md)

---

## กติกาสำหรับ agent ที่เป็นคนติดตั้ง

1. **อ่านหน้านี้ให้จบก่อนรันอะไรทั้งสิ้น** — คำถาม 5 ข้อในขั้นที่ 3 ไม่ใช่ของ optional
   และ installer ไม่ตอบแทนให้
2. **macOS เท่านั้น** — ถ้า `uname -s` ไม่ใช่ `Darwin` ให้หยุดแล้วบอก **ห้ามเดา path ของ OS อื่นเอง**
3. **ทุกอย่างที่เขียนนอก vault เป็น user scope — ต้องถามก่อนทีละชิ้น** เขาปฏิเสธข้อไหน
   ให้ข้ามข้อนั้นแล้วทำข้ออื่นต่อ **อย่าล้มทั้งกระบวนการ และอย่าตื๊อซ้ำ**
4. **ห้ามแก้เนื้อสกิล** — copy จาก `skills/` ตรง ๆ ห้ามย่อ ห้ามเรียบเรียงใหม่ ห้ามแปล
5. **ห้ามทับของเดิมเงียบ ๆ** — จะทับอะไร บอกก่อนทับ
6. **ทำตามลำดับ 1 → 5** ขั้นไหนล้มให้หยุดแล้วรายงานตามจริง
   **ห้ามเดินต่อแล้วสรุปว่าสำเร็จ**

## ของที่ต้องมีก่อน

- macOS และ **Node 18 ขึ้นไป** (`node -v`)
- `git`
- Obsidian — ตอนติดตั้งยังไม่มีก็ได้ · แต่ installer จะลงทะเบียน vault ให้ได้ก็ต่อเมื่อ
  เครื่องนี้เคยเปิด Obsidian มาแล้วอย่างน้อยหนึ่งครั้ง (ดูขั้นที่ 5)

---

## ติดตั้ง

### ขั้นที่ 1 — ตรวจ ยังห้ามเขียนอะไรทั้งสิ้น

```bash
git clone <url ของ repo นี้> ~/Documents/Git/wayfinder-template
```

```bash
cd ~/Documents/Git/wayfinder-template && node template/_tools/bootstrap.mjs --plan
```

`--plan` คือขั้น 1–3 ที่ **ไม่เขียนอะไรเลยสักไบต์**: ตรวจ → พิมพ์รายงาน → พิมพ์คำถาม 5 ข้อ → exit 0
ไม่สร้างอะไรเลย แม้แต่โฟลเดอร์ vault

> ⚠️ `--plan` เป็น dry run **เฉพาะกับเครื่องที่ยังไม่มี vault ติดตั้งอยู่** · ถ้า vault มี
> `.wayfinder-template.json` แล้ว flag นี้ **ถูกเมิน** แล้วรอบนั้นจะอัปเดตจริง ดู [อัปเดต](#อัปเดต)

### ขั้นที่ 2 — รายงานก่อนแตะอะไร

เอา output ของ `--plan` ให้เขาดูตามที่มันพิมพ์ออกมา — มันบอกสภาพเครื่องเขาว่า:

- เจอ template repo ที่ไหน commit ไหน
- vault ปลายทางคือ path ไหน และรอบนี้จะเป็นโหมด **install** หรือ **update**
- vault มีอยู่แล้วหรือยัง · เป็น git repo แล้วหรือยัง
- Obsidian ลงแล้วหรือยัง · และตอนนี้ **เปิดค้างอยู่ไหม**
- hook commit อัตโนมัติต่อไว้แล้วหรือยัง
- `~/.claude/CLAUDE.md` มีย่อหน้า wayfinder แล้วหรือยัง

**ยังไม่ต้องเสนอว่าจะทำอะไร** ขั้นนี้คือ "นี่คือสภาพเครื่องคุณ" เฉย ๆ

### ขั้นที่ 3 — ถาม 5 ข้อ ทีละข้อ รอคำตอบทุกข้อ

**1. vault จะวางที่ path ไหน?** → `--vault <path>`
ค่าเริ่มต้น `~/Documents/Git/wayfinder-vault` · จะวางที่ไหนก็ได้ ไม่จำเป็นต้องอยู่ใน
`~/Documents/Git` · vault เป็น git local อย่างเดียว **ไม่มี remote โดยตั้งใจ**

**2. จะติดตั้งชิ้นไหนบ้าง?** → `--parts a,b,c` (เลือกได้หลายข้อ · ค่าเริ่มต้น = ทุกข้อ)

| ชิ้น | ทำอะไร | เขียนที่ไหน |
|---|---|---|
| `skills` | สกิล `/wayfinder` และ `/wayfinder-next` | นอก vault (ดูข้อ 5) |
| `vault` | เนื้อ vault · `_tools/` · `git init` | ใน vault |
| `hook` | hook commit อัตโนมัติ | `~/.claude/settings.json` |
| `obsidian` | ค่าตั้ง `.obsidian/` · ตัวปลั๊กอิน Dataview · ลงทะเบียน vault | ใน vault + config ของ Obsidian เอง |
| `memory` | ย่อหน้า wayfinder ใน `~/.claude/CLAUDE.md` | `~/.claude/CLAUDE.md` |

> ⚠️ **ตอนถามข้อ `memory` ต้องพูดเรื่องนี้ออกมาตรง ๆ แล้วเคารพคำตอบ จบตรงนั้น**
>
> ย่อหน้าใน `~/.claude/CLAUDE.md` เป็นที่ **เดียว** ที่บอก `/wayfinder` ว่า vault อยู่ไหน
> ไม่มีย่อหน้านี้ พอเรียก `/wayfinder` ในrepo ไหนก็ตาม สกิลจะไปทำตามกติกา issue tracker
> ของ repo นั้นแทน (`docs/agents/issue-tracker.md` และพวกเดียวกัน) ⇒ map ถูกเขียนลงใน repo
> ไม่ใช่ลง vault แล้ว**ตายไปพร้อมกับ branch หรือ worktree ที่มันถูกเขียน**
> การปฏิเสธเป็นทางเลือกที่ชอบธรรม ราคาของมันคือต้องพิมพ์ path ของ vault บอกเองทุกครั้ง
>
> ถ้าเขาไม่เอาชิ้นนี้ installer จะพิมพ์ผลข้อนี้ออกมาซ้ำให้เองอีกรอบ · **มันไม่ถามซ้ำ และคุณก็อย่าถามซ้ำ**

**3. เจอของเดิมอยู่แล้วเอายังไง?** → `--on-conflict skip|backup`
ค่าเริ่มต้น `skip` (ปล่อยไฟล์เดิมไว้ ไม่ทับ) · อีกทางคือ `backup` — ก็อบไปเป็น `<ไฟล์>.bak` แล้วค่อยเขียนทับ
คำตอบข้อนี้ใช้กับ **install** เท่านั้น · vault ที่ติดตั้งแล้วเป็นอีกเรื่อง ดู [อัปเดต](#อัปเดต)

**4. อนุญาตให้รัน `brew install --cask obsidian` ไหม?** → `--allow-brew yes|no`
**ไม่มีค่าเริ่มต้น ข้อนี้ต้องได้คำตอบจริง ๆ** · มีผลเฉพาะตอนที่เลือก `obsidian` ในข้อ 2 แล้วเครื่องยังไม่มีแอป
ถ้าอยู่ในสถานการณ์นั้นแล้วยังไม่มีคำตอบ installer จะ**หยุด** ไม่เดาให้
ส่วน Dataview ไม่ต้องถาม — ship มากับ repo นี้แล้ว วางจากที่นี่เลย

**5. สกิลจะวางที่ไหน?** → `--skills-dir <path>`
ค่าเริ่มต้น `~/.claude/skills` · ถ้า `<dir>/<ชื่อสกิล>` เป็น **symlink** อยู่แล้ว installer จะ
เดินตามไปทับที่ปลายทาง ไม่ทำลาย layout เดิมของเขา

> **ทำไม installer ถึงไม่ยอมเดาคำตอบให้:** ในโหมด install ถ้า stdin ไม่ใช่ terminal
> (= agent เป็นคนรัน) และไม่มี `--parts` หรือ `--yes` ส่งมา มันจะพิมพ์คำถามแล้ว **exit 2**
> — นี่คือพฤติกรรมที่ตั้งใจ ไม่ใช่บั๊ก มันคือกลไกที่กันไม่ให้ agent ไปติดตั้งกระจายทั่วเครื่องคนอื่น
> โดยไม่ถาม · `--vault` เดี่ยว ๆ **ไม่นับว่าตอบครบ** มันตอบแค่ข้อ 1 ไม่ใช่ใบอนุญาตให้เดาอีกสี่ข้อ

### ขั้นที่ 4 — ลงมือเฉพาะที่เขาเลือก

เอาคำตอบมาใส่เป็น flag · ตัวอย่างของคนที่รับค่าเริ่มต้นทุกข้อ และตอบข้อ 4 ว่าไม่:

```bash
node template/_tools/bootstrap.mjs --vault ~/Documents/Git/wayfinder-vault --parts all --on-conflict skip --skills-dir ~/.claude/skills --allow-brew no
```

ถ้าเขาปฏิเสธชิ้นไหน ให้ใส่เฉพาะชิ้นที่เขารับ — เช่น `--parts skills,vault,hook`
จะไม่แตะ `~/.claude/CLAUDE.md` และไม่แตะ Obsidian เลย

`--yes` แปลว่า "เขาตอบแล้ว และเลือกค่าเริ่มต้นทุกข้อ" · ใช้ได้ต่อเมื่อเป็นอย่างนั้นจริง ๆ

ระหว่างรันมันจะพิมพ์ให้เห็นเอง: เขียนไปกี่ไฟล์ ข้ามกี่ไฟล์ · สกิลลงที่ไหน (รวมถึง symlink ที่มันเดินตาม) ·
hook ต่อใหม่หรือมีอยู่แล้ว · และจดอะไรลง manifest บ้าง · ปิดท้ายมัน commit ลง vault ให้
เพื่อให้ working tree สะอาด

### ขั้นที่ 5 — ตรวจ แล้วสรุปตามจริง

```bash
node ~/Documents/Git/wayfinder-vault/_tools/doctor.mjs
```

**exit 0 = ผ่านหมดทุกข้อ** · บรรทัดที่ขึ้น `⚠️` เป็นคำเตือน ไม่ใช่ข้อที่ตก — vault ที่เพิ่งติดตั้ง
ยังไม่มี map จะขึ้นเตือนสองสามข้อ และนั่นถูกแล้ว · installer รัน `doctor.mjs` ให้เองตอนจบอยู่แล้ว
ผลจึงอยู่บนจอแล้ว · ให้รันซ้ำอีกทีหลังทำขั้นตอนมือข้างล่างเสร็จ

มีสองผลที่ควร **คาดไว้ล่วงหน้า** แทนที่จะตกใจตอนเห็น:

- **ข้อ "Obsidian ลงทะเบียน vault นี้ไว้แล้ว" จะแดง ถ้าเครื่องนั้นไม่เคยเปิด Obsidian มาก่อน**
  — installer ลงทะเบียน vault ได้ทางเดียวคือแก้ไฟล์ config ของ Obsidian เอง ซึ่งไฟล์นั้น
  **ยังไม่เกิดจนกว่าแอปจะถูกเปิดหนึ่งครั้ง** · แก้ได้สองทาง เลือกทางไหนก็เขียว:
  เปิด Obsidian แล้วใช้ *Open folder as vault* หรือเปิด Obsidian หนึ่งครั้งแล้วรัน installer ซ้ำ
- **hook ต้องให้ Claude Code โหลด config ใหม่ก่อนถึงจะเริ่มยิง** — ดูขั้นตอนมือข้างล่าง

แล้วรายงาน: ติดตั้งอะไรไปบ้าง · เขาปฏิเสธข้อไหน · `doctor.mjs` ว่ายังไง · เหลือขั้นตอนมือข้อไหน

### ติดตั้งลง vault ที่มีอยู่ก่อนแล้ว

ถ้าปลายทางเป็น vault wayfinder อยู่แล้ว — ตัวที่เกิดก่อน repo นี้ — การติดตั้งครั้งนั้นคือ
**การย้ายบ้าน** และคำถามข้อ 3 คุมน้อยกว่าที่ถ้อยคำของมันชวนให้คิด:

- `_tools/` กับสกิล **ถูกเขียนไม่ว่าตอบอะไร** — สองอย่างนี้เป็น *โปรแกรม* ไม่ใช่ *เอกสาร*
  vault ที่ยังรัน `bootstrap.mjs` รุ่นเก่าจะหยุด converge ในทุกการอัปเดตถัดไปโดยไม่มีอะไรเตือน
  · ตัวรันพิมพ์ชื่อไฟล์ที่ทับออกมาทุกตัว · ของใน vault กู้จาก git ได้ ส่วนสกิลอยู่นอก git
  ⇒ ใช้ `--on-conflict backup` ถ้าอยากได้ `.bak` ไว้
- **เอกสาร** — `README.md` · `SETUP.md` · สามโน้ต Wayfinder — เดินตามคำตอบจริง
  ค่าเริ่มต้น `skip` จึงปล่อยไว้เหมือนเดิมทุกตัวอักษร แล้วมันจะไปตรงกันตอน **อัปเดตครั้งแรก**
  ซึ่งเป็นคำสั่งเดียวกับที่ทุกคนใช้
- `example-repo/` ไม่ถูกวางเลยถ้า vault มี effort จริงอยู่แล้ว และไม่ถูกจดลง manifest
  ⇒ ไม่มีอัปเดตรอบไหนปลุกมันคืน

การย้ายบ้านจึงเป็นสองคำสั่ง ไม่ใช่คำสั่งเดียว: install แล้วต่อด้วย update

---

## ขั้นตอนมือที่สคริปต์ทำแทนไม่ได้

1. เปิด Obsidian → **Open folder as vault** → เลือก path ของ vault
2. Settings → **Community plugins** → *Turn on community plugins*
   ตัวปลั๊กอิน Dataview · ค่าตั้งของมัน · สี Graph View มากับ repo นี้แล้ว **ไม่ต้องโหลดอะไรเพิ่ม**
3. เปิดโน้ต **Wayfinder Dashboard** กับ **Wayfinder Efforts** ค้างไว้ — นั่นคือหน้างานจริง
4. ถ้าเพิ่งต่อ hook: เปิด `/hooks` ใน Claude Code หนึ่งครั้ง หรือรีสตาร์ตมัน ให้ config โหลดใหม่

---

## อัปเดต

```bash
cd ~/Documents/Git/wayfinder-template && git pull
```

```bash
node ~/Documents/Git/wayfinder-vault/_tools/bootstrap.mjs --from ~/Documents/Git/wayfinder-template
```

**update ไม่ถามอะไรเลย** — คำตอบ 5 ข้อจากตอน install ถูกจดไว้ใน manifest ของ vault
(`.wayfinder-template.json`: ติดตั้งชิ้นไหน · สกิลไปลงที่ไหน · เรนเดอร์ path ไหนเข้าไป)
แล้วเอามาใช้ตามนั้น · update ที่ถามใหม่ = update ที่มีโอกาสถูกตอบผิดจากเดิมโดยไม่ตั้งใจ

จะรันจากฝั่ง repo แทนก็ได้ (`node template/_tools/bootstrap.mjs --vault <vault>`) ผลเหมือนกัน —
**โหมดตัดสินจากว่า vault มี manifest แล้วหรือยัง ไม่ใช่จากว่ารันสำเนาไหนของสคริปต์**

### อะไรถูกทับ

`template/` **ทั้งชุด** — สามโน้ต Wayfinder · `README.md` · `SETUP.md` · `_tools/` ·
โค้ดปลั๊กอิน Dataview — ถูกทับเป็น **หน่วยเดียว** ไม่ใช่ทีละไฟล์ และไม่ใช่ตัวเลือก:
สามโน้ตอ้างค่าคงที่ของกันและกัน และ `doctor.mjs` เช็คว่ามันตรงกัน ⇒ อัปเดตครึ่ง ๆ
= ได้ vault ที่ตกการตรวจสุขภาพของตัวเอง ทั้งที่ไม่มีใครทำอะไรผิดสักคน

ไฟล์ที่ repo นี้ **เลิก ship แล้ว** จะถูกลบออกจาก vault จริง ๆ ด้วย · การลบนั้นขับด้วย
**manifest รอบก่อนอย่างเดียว** — ไฟล์ที่ไม่เคยอยู่ใน manifest ไม่มีทางเดินเข้าลูปลบนั้นได้เลย

### อะไรที่ไม่ถูกแตะ

| | |
|---|---|
| `<repo>/<effort>/` — ทุก map ทุก ticket ทุก asset | งานจริงของคุณ |
| `Wayfinder Config.md` | ค่าที่คุณจูน: เส้นแบ่ง "นิ่งมานาน" · สี · ป้ายสถานะ |
| `Wayfinder Picks.md` | กฎ "หยิบอันไหนต่อ" ที่คุณเขียนเอง |
| `example-repo/` | วางตอน install เท่านั้น · ลบไปแล้วไม่ถูกปลุกคืน |
| ค่าตั้งของคุณใน `.obsidian/` | **โค้ด**ของปลั๊กอิน Dataview อัปเดตได้ · **ค่าตั้ง**ไม่อัปเดต — ทับเมื่อไหร่จะไปปิดปลั๊กอินอื่นที่คุณเปิดไว้เงียบ ๆ และสี Graph View ที่คุณปรับเองจะหาย |
| ของที่คุณเพิ่มเองไม่ว่าที่ไหน | ตัวอัปเดตเดินตามรายชื่อใน manifest ไม่ได้กวาดหาของแปลกปลอม |

⇒ จะปรับค่าอะไร **ปรับใน `Wayfinder Config.md`** ไม่ใช่ในบล็อกโค้ดของสามโน้ต ·
ของที่แก้ในสามโน้ตจะหายในการอัปเดตครั้งถัดไป และนั่นคือพฤติกรรมที่ตั้งใจ

ถ้าคุณเคยแก้ไฟล์ฝั่ง managed ไว้จริง ตัวอัปเดตจะ**พิมพ์ชื่อไฟล์นั้นออกมา**บอกว่าทับไปแล้ว

### ถ้าอัปเดตแล้วไม่ชอบ จะกู้คืนยังไง

vault เป็น git repo และ installer commit ให้ทุกรอบที่รัน ⇒ ของเดิมอยู่ใน history เสมอ:

```bash
cd ~/Documents/Git/wayfinder-vault && git log --oneline
```

แล้ว `git revert <commit ที่ขึ้นว่า "chore(vault): update wayfinder template …">` ·
ถ้าอยากได้คืนแค่ไฟล์เดียว `git log -p -- "<ไฟล์>"` จะให้เห็นว่าก่อนหน้านี้มันหน้าตายังไง

ส่วนไฟล์ที่เขียน **นอก vault** จะถูกก็อบเก็บไว้ข้าง ๆ ตัวเองในครั้งแรกที่ถูกแก้ **ถ้าไฟล์นั้นมีอยู่ก่อน**:
`~/.claude/settings.json.bak` · `~/.claude/CLAUDE.md.bak` · และ `obsidian.json.bak` ของ Obsidian
(ถ้าเดิมไม่มีไฟล์อยู่ ก็ไม่มีอะไรให้สำรอง จะไม่มี `.bak` โผล่มา)

---

## รันซ้ำแล้วเกิดอะไรขึ้น

installer รันซ้ำได้ปลอดภัย — และนี่เป็นคุณสมบัติที่คุณต้องเชื่อได้ก่อน ถึงจะกล้ารันมันตั้งแต่แรก

- รันรอบที่สองบน vault ที่ติดตั้งแล้ว **คือ update** ไม่ว่าจะส่ง flag อะไรไปก็ตาม ·
  ไฟล์ที่เนื้อตรงกันอยู่แล้วถูกข้าม · ไม่เขียนอะไรเลย · และ**ไม่เกิด commit เปล่า**
- hook ต่อครั้งเดียว · รันซ้ำมันเจอทั้งสองเส้นแล้วข้าม
- ย่อหน้าใน `~/.claude/CLAUDE.md` เติมครั้งเดียว · รันซ้ำมันเจอแล้วข้าม
- `Wayfinder Config.md` · `Wayfinder Picks.md` · effort ของคุณ ไม่ถูกแตะทุกรอบ

ข้อเดียวที่ **ห้ามคิดเอาเอง**: `--plan` ไม่ได้ป้องกัน vault ที่ติดตั้งไปแล้ว
มันเป็น dry run ของโหมด install เท่านั้น

---

## หลังติดตั้งเสร็จแล้ว

การตรวจสุขภาพ · วิธีแก้ปัญหา · และการดูแล vault ที่ติดตั้งแล้ว อยู่ใน `SETUP.md` ของ vault เอง
ซึ่ง installer วางไว้ที่รากของ vault · หน้านี้ว่าด้วยการเอา vault ขึ้นเครื่อง
และการทำให้มันเดินตาม repo นี้เท่านั้น
