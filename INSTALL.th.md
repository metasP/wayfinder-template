# ติดตั้งและอัปเดต

> **🚧 ยังเป็น stub — ไฟล์นี้เขียนโดยใบ `08-install-doc-bilingual`**
> มันติด `bootstrap.mjs` (ใบ `03`) ที่ยังเขียนไม่เสร็จ ⇒ **อย่าเดาคำสั่งจากหน้านี้**
> ตั้งใจไม่เขียนคำสั่งไว้ ดีกว่าเขียนผิดแล้วมีคนก็อบไปรัน

หน้านี้จะครอบเรื่องพวกนี้:

1. **clone** repo นี้ แล้วบอก agent ว่า *"ติดตั้ง wayfinder จาก repo นี้"*
2. installer **ถามก่อนแตะทุกอย่างที่อยู่นอก vault** ตามโปรโตคอล 5 ขั้น
   (ตรวจ → รายงาน → ถาม → ลงมือเฉพาะที่เลือก → ตรวจแล้วสรุป)
   รวมถึงถามว่า **จะวางสกิลไว้ที่ไหน** (default `~/.claude/skills/` · ถ้าเจอ symlink ให้เดินตามไปทับปลายทาง)
3. มันสร้าง `Wayfinder Config.md` กับ `Wayfinder Picks.md` ให้ — **สองใบนี้เป็นของคุณ**
   การอัปเดตครั้งไหนก็ไม่แตะอีกเลย
4. มันเติมย่อหน้า wayfinder ลง `~/.claude/CLAUDE.md` — **นี่เป็นที่เดียวที่บอก `/wayfinder` ว่า
   vault อยู่ไหน** ไม่เติมแล้วสกิลจะไปทำตามกติกา issue tracker ของ repo ที่เปิดอยู่แทน แล้ว map
   จะไม่ลง vault
5. **อัปเดต:** `git pull` ที่นี่ แล้วบอก agent ว่า *"update"* · `template/` ถูกทับทั้งชุด
   ส่วน effort ของคุณกับสองไฟล์ในข้อ 3 ไม่ถูกแตะ

การตรวจสุขภาพและวิธีแก้ปัญหาของ vault ที่ติดตั้งแล้ว อยู่ใน `SETUP.md` ของตัว vault เอง
(ship อยู่ที่ `template/SETUP.md`)
